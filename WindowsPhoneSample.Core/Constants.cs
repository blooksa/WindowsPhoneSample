using System;

namespace WindowsPhoneSample.Core
{
    public static class Constants
    {
        public const string HmsfFormat = "HH:mm:ss.ff";
        public static readonly TimeSpan DefaultTimeout = TimeSpan.FromSeconds(5);
    }
}